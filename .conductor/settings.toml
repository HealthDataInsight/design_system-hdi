"$schema" = "https://conductor.build/schemas/settings.repo.schema.json"

[scripts]
setup = "bin/conductor-setup"
run_mode = "concurrent"

[scripts.run.dev]
command = "cd test/dummy && PORT=$CONDUCTOR_PORT bin/dev"
default = true
icon = "play"
