module DesignSystem
  module Hdi
    # HDI action link. Follows the NHS action-link pattern but swaps in the
    # HDI arrow icon, so it ships its own template.
    class ActionLinkComponent < DesignSystem::Nhsuk::ActionLinkComponent
    end
  end
end
