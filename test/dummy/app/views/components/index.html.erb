<%=
ds_fixed_elements do |ds|
  ds.main_heading 'Components'

  ds.breadcrumb('Home', root_path)

  ds.lead_paragraph 'Components are reusable elements of the user interface.'
end
%>

<% content_for :sidebar do %>
  <%= render partial: 'sidebar' %>
<% end %>
