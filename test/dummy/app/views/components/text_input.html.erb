<%=
ds_fixed_elements do |ds|
  ds.main_heading 'Text input', caption: 'Form elements'

  ds.breadcrumb('Home', root_path)
  ds.breadcrumb('Components', components_path)

  ds.lead_paragraph 'Use text input to let users enter a single line of text.'
end
%>

<%= component_preview fragment: :form_group do %>
<%%= ds_form_with(model: Assistant.new) do |f|
  f.ds_text_field :title, width: 20, hint: "Demo for ds_text_field", class: "#{brand}-input--width-30"
end %>
<% end %>

<%= component_preview :text_input_phone, fragment: :form_group do %>
<%%= ds_form_with(model: Assistant.new) do |f|
  f.ds_phone_field :phone, width: 20, hint: "Demo for ds_phone_field"
end %>
<% end %>

<%= component_preview :text_input_email, fragment: :form_group do %>
<%%= ds_form_with(model: Assistant.new) do |f|
  f.ds_email_field :email, width: 20, hint: "Demo for ds_email_field"
end %>
<% end %>

<%= component_preview :text_input_url, fragment: :form_group do %>
<%%= ds_form_with(model: Assistant.new) do |f|
  f.ds_url_field :website, hint: "Demo for ds_url_field"
end %>
<% end %>

<%= component_preview :text_input_number, fragment: :form_group do %>
<%%= ds_form_with(model: Assistant.new) do |f|
  f.ds_number_field :age, width: 3, hint: "Demo for ds_number_field"
end %>
<% end %>

<%#= ds_heading 'Width classes', level: 3 %>
<%#= ds_inset_text 'TODO' %>
