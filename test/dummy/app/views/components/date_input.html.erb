<%=
ds_fixed_elements do |ds|
  ds.main_heading 'Date input', caption: 'Form elements'

  ds.breadcrumb('Home', root_path)
  ds.breadcrumb('Components', components_path)

  ds.lead_paragraph 'Use date input to help users enter a memorable date, like their date of birth.'
end
%>

<%= component_preview fragment: :form_group do %>
<%%= ds_form_with(model: Assistant.new) do |f|
  f.ds_date_field :date_of_birth, hint: "Demo for ds_date_field", date_of_birth: true, legend: { size: nil }
end %>
<% end %>
