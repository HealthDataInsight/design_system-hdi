<%=
ds_fixed_elements do |ds|
  ds.main_heading 'Password input', caption: 'Form elements'

  ds.breadcrumb('Home', root_path)
  ds.breadcrumb('Components', components_path)

  ds.lead_paragraph 'Help users to create and enter passwords.'
end
%>

<%= component_preview fragment: :form_group do %>
<%%= ds_form_with(model: Assistant.new) do |f|
  f.ds_password_field :password, hint: "Demo for ds_password_field", class: "#{brand}-input--width-30"
end %>
<% end %>
